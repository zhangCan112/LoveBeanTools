import React from 'react';
import {
    Button
} from 'antd';


BigButton.propTypes = {

};

export default class BigButton extends React.Component {
    render() {
        return (
            <Button type={"primary"}>
                
            </Button>
        )
    }
}